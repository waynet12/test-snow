{
  "name": "hello-world-fluent",
  "version": "1.0.0",
  "description": "Simple Hello World Fluent app for ServiceNow",
  "private": true,
  "scripts": {
    "build": "now-sdk build",
    "install:app": "now-sdk install"
  },
  "devDependencies": {
    "@servicenow/sdk": "^4.2.0",
    "@servicenow/glide": "^27.0.3",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=18.16.1"
  }
}
